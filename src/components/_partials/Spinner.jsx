
import '../../css/styles.css'; // Asegúrate de que este archivo CSS esté en la misma carpeta o ajusta la ruta según sea necesario

function Spinner() {
  return (
    <div className="spinner"></div>
  );
}

export default Spinner;
